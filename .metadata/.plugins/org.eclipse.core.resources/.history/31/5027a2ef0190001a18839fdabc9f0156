<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>JavaScript 객체지향</title>
<script type="text/javascript">
//1. 클래스 선언 - 생성자로 초기화하기
// 생성자명은 일반적으로 대문자로 시작하기

var Student = function(name,kor,eng,math){
	//클래스선언
	this.name=name;
	this.kor=kor;
	this.eng=eng;
	this.math=math;
}

//클래스에 추가하면 s1/s2 모두 사용가능*** 클래스 선언함수 밖에서 사용하려면 this로 멤버변수 확인 
// Student.prototype.info=function(){
// 	return this.name+"님의 평균 : "+this.sum()/3+"<br/>";
// };

// Student.prototype.sum=function(){
// 	return this.kor+this.eng+this.math;
// };


Student.prototype={
	sum function(){
	 	return this.name+"님의 평균 : "+this.sum()/3+"<br/>";
	};
	
	info function(){
	 	return this.kor+this.eng+this.math;
	 };	
};

//2. 객체 생성
var s1=new Student('홍길자',99,66,77);
document.write(s1.name+"님의 총점 : "+s1.sum()+"<br/>");

//s1만 사용가능***
// s1.info=function(){
// 	return this.name+"님의 평균 : "+this.sum()/3+"<br/>";
// };

document.write(s1.info());

var s2=new Student('홍길숙',99,66,77);
document.write(s2.name+"님의 총점 : "+s2.sum()+"<br/>");

document.write(s2.info());//s2에 영향x

</script>
</head>
<body>

</body>
</html>